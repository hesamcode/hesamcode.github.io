# HesamCode Portfolio (FA/EN) — Next.js + Tailwind

A bilingual (FA/EN) portfolio web app built with Next.js App Router and Tailwind CSS, optimized for static export and GitHub Pages.

🌐 Live: https://hesamcode.github.io/

---

## Features

- ✅ **Bilingual** (Persian / English) with separate routes: `/fa/` and `/en/`
- ✅ **Modern UI** with Tailwind + CSS variables (Dark/Light theme)
- ✅ **SEO-ready**: metadata, canonical, hreflang, OpenGraph, Twitter cards, JSON-LD
- ✅ **Collaboration form**: project type + budget + message builder (WhatsApp / Telegram)
- ✅ **Projects with filters** + responsive layout
- ✅ **FAQ section** outside tabs
- ✅ **Static export** (`output: export`) compatible with GitHub Pages

---

## Tech Stack

- Next.js (App Router)
- TypeScript
- Tailwind CSS
- Font Awesome (icons)
- Static Export for GitHub Pages

---

## Project Structure

- `src/app/` → App Router pages (`/fa`, `/en`)
- `src/components/` → UI components (PortfolioApp, ThemeInit, ...)
- `src/data/` → i18n + projects + collaboration data
- `public/` → static assets (icons, images, manifest, sw, ...)

---

## Development

Install dependencies:

```bash
npm install
```
